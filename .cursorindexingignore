# Patterns to exclude from indexing only (still accessible via @ reference)
# These files save indexing tokens but remain available when explicitly referenced

# Ignore all third_party directories from indexing
**/third_party/

# Build artifacts (not indexed but accessible when needed)
**/dist/
**/dist_*/
**/build/
**/out/



# Archives/binaries (large files, don't need indexing but may reference)
*.tgz
*.tar
*.zip
*.gz
*.bz2
*.7z
*.jar
*.war
*.zstd
*.xray

# Test data directories (large test datasets, not indexed but may reference)
**/test_data/
**/testdata/

# ML frameworks and training scripts (third-party frameworks, not indexed but may reference)
/offboard/weather/train_scripts/fouling_dl/mmpretrain-weather/

# Jupyter notebooks (may want to reference but not index)
*.ipynb

# Binary/model/media assets (don't need text indexing but may reference)
**/binary/
*.bin
*.onnx
*.trt
*.tflite
*.protobuf
*.pb
*.pbtxt
*.png
*.jpg
*.jpeg
*.svg
*.gif
*.webp
*.ico
*.bmp
*.mp4
*.mov
*.avi
*.mp3
*.wav
*.pdf

# Compiled objects and libs (may reference when debugging but don't index)
*.o
*.a
*.so
*.dylib
*.exe

# JS/TS lockfiles (may need to check but don't index)
yarn.lock
pnpm-lock.yaml
